{
  "devDependencies": {
    "postcss": "^8.2.2",
    "postcss-cli": "^8.3.1",
    "postcss-preset-env": "^6.7.0"
  },
  "scripts": {
    "build": "hugo --gc --minify",
    "dev": "hugo server -D"
  },
  "prettier": {
      "tabWidth": 4
  }
}
